# Browser review

2026-09-12: opened the generated self-contained lab through a loopback HTTP server in Codex's in-app browser. Inspected the rendered interface and complete underlying state after unsigned import, signed import and changed same-version rejection. Execute remained blocked without local binding/grant. Selected interrupted-import evidence (after-blobs process exit) and public-redaction evidence; each showed its matching retained outcome and expanded snapshot. A screenshot confirmed readable controls, bounded scrollable state and no visual overlap in the observed viewport.

This validates presentation and illustrative reducer interaction only. Actual parser and durable evidence comes from the Python probe; the HTML model does not independently certify that implementation. The lab embeds the final 75-scenario main report. No user approval or usability endorsement is inferred from opening it.
