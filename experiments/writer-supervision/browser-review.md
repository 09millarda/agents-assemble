# Browser lab inspection

The self-contained HTML was served over loopback for inspection in the Codex in-app browser. The parent-exit walkthrough correctly refused replacement after parent death and interrupt acknowledgment. Free play retained a pause after exact payload emptiness and checkpoint verification while effects remained unknown. Reconciling effects allowed invocation-2; applying invocation-1 receipt left that replacement active. Visual inspection confirmed readable panels and unclipped controls at the app viewport.

The final embedded protocol payload was selected in the browser and read as 90/90 scenarios with source hash ff1c003035d012260b9d65c14064622de09e2c5311c4d27940656c7d04fba736. Browser clicks are illustrative model verification, not empirical process/protocol scenarios.

Direct file-URL navigation was rejected by the in-app browser URL policy. No workaround was attempted. The artifact remains a standalone HTML file; observed browser verification used its existing loopback preview.
