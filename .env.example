POSTGRES_USER=factory
POSTGRES_PASSWORD=factory
POSTGRES_DB=factory
POSTGRES_PORT=5432
DATABASE_URL=postgres://factory:factory@localhost:5432/factory
PORT=3001
COORDINATOR_PORT=3003
VITE_FACTORY_API_URL=http://localhost:3001
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3002

# Generate once and retain these keys across API/coordinator restarts.
# Generate with: pnpm --filter @factory/coordinator exec web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:operator@example.com
