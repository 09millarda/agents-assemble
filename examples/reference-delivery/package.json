{
  "name": "agents-assemble-reference-delivery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit --strict --skipLibCheck --target ES2023 --module ESNext --moduleResolution Bundler --allowImportingTsExtensions app.ts handler.ts app.test.ts"
  },
  "dependencies": {
    "hono": "4.13.7",
    "zod": "4.6.4"
  },
  "devDependencies": {
    "@types/node": "24.0.0",
    "esbuild": "0.27.0",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
